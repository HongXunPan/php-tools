**简体中文** | [English](SECURITY.en.md)

# 安全策略

我们重视 `hongxunpan/php-tools` 及其使用者的安全。请负责任地披露潜在漏洞，避免在修复可用前公开可利用细节。

## 支持范围

| 包版本 | 安全维护状态 | PHP 要求 |
| --- | --- | --- |
| `3.x` | 正常维护 | PHP `>=5.6` |
| `2.x` | 历史版本，仅按影响评估处理 | 以对应版本约束为准 |
| `<2.0` | 不再维护 | 不适用 |

PHP `>=5.6` 是当前主版本的正式兼容边界，但 PHP 运行时自身的生命周期与安全维护不由本项目控制。生产环境仍应在业务兼容范围内选用受上游安全维护的 PHP 版本。

## 报告漏洞

请优先通过以下任一私密渠道报告：

1. GitHub 仓库的“私密漏洞报告”功能（若仓库已启用）；
2. 发送邮件至 `me@kangxuanpeng.com`，主题建议为“[php-tools 安全报告] 简要标题”。

请不要通过公开 Issue、Pull Request、讨论区或社交媒体披露未修复漏洞。

## 报告内容

为便于复现和评估，请尽量提供：

- 受影响的包版本、PHP 版本与运行环境；
- 漏洞类型、影响范围和必要前提；
- 最小复现步骤或概念验证；
- 可能的攻击路径与实际影响；
- 已知缓解措施或修复建议；
- 你的署名与披露偏好。

提交前请移除真实密钥、个人数据和生产环境凭据。

## 处理流程

维护者会尽快确认报告，并按以下流程推进：

1. 验证问题并确定受影响版本；
2. 评估严重性、利用条件和兼容性影响；
3. 在私密范围内准备修复与回归测试；
4. 协调发布时间、公告范围和必要的升级指引；
5. 发布修复后，在不增加使用者风险的前提下披露必要信息。

修复时间取决于复杂度、兼容性和发布风险。处理期间如有重要进展，维护者会通过原报告渠道同步。

## 安全研究边界

欢迎报告由本包实现直接导致的安全问题，例如路径处理、加密语义、锁与并发、输入处理或依赖链风险。以下情况通常不视为本项目漏洞：

- 明确违反文档安全要求的配置或用法；
- 仅影响已停止维护版本且无法在受支持版本复现的问题；
- 业务系统自身的权限、部署或基础设施配置问题；
- 缺少可验证影响的自动化扫描结果。

即使不能确定问题是否属于本项目，也建议先通过私密渠道联系维护者。

## 致谢

在报告者同意的情况下，项目可在安全公告或更新日志中致谢。未经许可不会公开报告者身份或报告细节。
