**简体中文** | [English](CONTRIBUTING.en.md)

# 参与贡献

感谢你愿意改进 `hongxunpan/php-tools`。本项目以稳定、可复用和长期兼容为优先目标，所有变更都必须保持 PHP `>=5.6` 的正式支持。

## 开始之前

- 使用问题与一般疑问请先阅读 [支持说明](SUPPORT.md)；
- 安全问题请按 [安全策略](SECURITY.md) 私下报告，不要创建公开 Issue；
- 缺陷与功能建议请优先使用仓库提供的 Issue 模板；
- 较大的公共 API、依赖或兼容性变更，请先通过 Issue 说明动机、方案和迁移影响。

## 开发环境

1. Fork 仓库并从最新 `main` 创建短生命周期分支；
2. 安装与 PHP 版本兼容的 Composer；PHP 5.6 环境必须使用 Composer 2.2 LTS；
3. 安装依赖：

```bash
composer install
```

本项目通过 Composer 的 `config.platform.php=5.6.0` 固定最低依赖解析边界。不要为了本地环境方便而提高该版本。

## 兼容性约束

- 公共包继续支持 PHP `>=5.6`，不得引入 PHP 7 或 PHP 8 专属语法；
- 不得使用标量类型声明、返回类型、可空类型、空合并运算符等 PHP 5.6 不支持的语法；
- 新依赖必须同时满足 PHP 5.6、许可证兼容、维护状态和供应链风险要求；
- 新增或调整公共 API 时，必须说明向后兼容性和迁移方式；
- 代码注释、维护文档和提交说明应清晰解释“为什么”，避免只复述实现。

## 代码与测试

- 优先沿用仓内已有命名、目录和测试组织方式；
- 修复缺陷时应先增加能够复现问题的测试，再完成修复；
- 新能力至少覆盖正常路径、边界条件和失败路径；
- 不要提交 `vendor/`、IDE 工作区、密钥、日志或本地临时文件；
- 不要在同一个 Pull Request 中混入无关重构或格式化。

提交前至少运行：

```bash
composer validate --strict
composer lint
composer test
```

GitHub Actions 会在 PHP 5.6、7.4、8.0 和 8.5 上执行兼容性验证。单一新版本环境通过不能替代完整兼容矩阵。

## 文档与更新日志

- 面向使用者的行为、配置或公共 API 变化必须同步更新 README 或 `readme/` 文档；
- 已存在双语版本的公开入口发生语义变化时，应同步更新中英文；确实无需更新时须在 Pull Request 中说明；
- 用户可感知的变更应写入 [更新日志](CHANGELOG.md) 的“未发布”章节；
- 破坏性变更必须提供迁移说明，不得只在代码或提交信息中说明；
- 文档示例必须可执行，并避免包含真实凭据或敏感业务数据。

## 提交与 Pull Request

建议使用单一目的、可独立审阅的提交。提交标题可采用以下类型：

- `feat`：新增能力；
- `fix`：修复缺陷；
- `docs`：仅文档变更；
- `test`：测试变更；
- `refactor`：不改变外部行为的重构；
- `chore`：维护性工作。

Pull Request 应完整填写模板，至少说明：

1. 问题与目标；
2. 实现方式和影响范围；
3. PHP 5.6 与公共 API 兼容性；
4. 已执行的验证；
5. 文档、更新日志和安全影响。

维护者可能要求拆分范围、补充测试或调整设计。所有必需检查通过并完成审阅后，变更才会合并。

## 许可证

提交贡献即表示你有权提供相关内容，并同意其按本仓库的 [MIT License](LICENSE) 发布。
